<script type="text/javascript">

jQuery(document).ready(function() {
    
    jQuery("#<%= @place %>").roomsTable({
        
        selectNotifyMethod : <%= @selectNotifyMethod %>,
        root_url : "<%= @root_url %>",
        baseUrl : "<%= @baseUrl %>",
        getGridData : "<%= @getGridData %>",
        
        delayed : <%= @delayed %>,
        
        pageSize : <%= @pageSize %>,
        pageList : <%= @pageList %>,
        search : <%= @search %>,
        showRefresh : <%= @showRefresh %>,
        cardView : <%= @cardView %>,
        
        toolbar : "<%= @toolbar %>",
        controlDiv : "<%= @controlDiv %>",
        
        extraClause : "<%= @extraClause %>",
        
        modelType : <%= @modelType %>,
        modelTemplate : "<%= @modelTemplate %>",

        modal_create_title  : '<%= t "create-room" %>',
        modal_edit_title    : '<%= t "edit-room" %>',
        confirm_content     : '<%= (t "delete-selected-room").html_safe %>',
        confirm_title       : '<%= t "delete-confirmation" %>',
        
        name                : '<%= t "name" %>',
        purpose             : '<%= t "purpose" %>',
        comment             : '<%= t "comments" %>',
        sort_order          : '<%= t "sort-order" %>',
        
        showControls        : <%= @showControls %>,
        default_setup       : '<%= t "default_room_setup" %>',
        capacity            : '<%= t "capacity" %>',
        show_setup          : <%= @showSetup %>

    });
});

</script>
