<script type="text/javascript">

jQuery(document).ready(function() {
    jQuery("#rooms").roomTable({
        caption : '<%= t @caption %>',
        selectNotifyMethod : <%= @selectNotifyMethod %>,
        clearNotifyMethod : <%= @clearNotifyMethod %>,
        loadNotifyMethod : <%= @loadNotifyMethod %>,
        root_url : "<%= @root_url %>",
        baseUrl : "<%= @baseUrl %>",
        getGridData : "<%= @getGridData %>",
        
        view : <%= @view %>,
        search : <%= @search %>,
        del : <%= @del %>,
        edit : <%= @edit %>,
        add : <%= @add %>,
        refresh : <%= @refresh %>,
        pager : "<%= @pager %>",
        
        name : [<%= @name %>, '<%= t "Name"%>' ],
        setup : [<%= @setup %>, '<%= t "default_room_setup"%>' ],
        capacity : [<%= @capacity %>, '<%= t "Capacity"%>' ],
        purpose : [<%= @purpose %>, '<%= t "Purpose"%>' ],
        comment : [<%= @comment %>, '<%= t "comments"%>' ],
        
        multiselect : <%= @multiselect %>,
        
        extraClause : "<%= @extraClause %>",
        
        filtertoolbar : false,
        
        modelType : <%= @modelType %>,
        showControls : <%= @showControls %>,
        
        modal_create_title : '<%= t "Create Room" %>',
        modal_edit_title : '<%= t "Edit Room" %>',
        
        sortname : 'rooms.name'
    });
});

</script>
