<script type="text/javascript">

jQuery(document).ready(function() {
    
    jQuery("#<%= @place %>").venuesTable({
        
        selectNotifyMethod : <%= @selectNotifyMethod %>,
        root_url : "<%= @root_url %>",
        baseUrl : "<%= @baseUrl %>",
        getGridData : "<%= @getGridData %>",
        
        delayed : <%= @delayed %>,
        
        pageSize : <%= @pageSize %>,
        pageList : <%= @pageList %>,
        search : <%= @search %>,
        showRefresh : <%= @showRefresh %>,
        cardView : <%= @cardView %>,
        
        toolbar : "<%= @toolbar %>",
        controlDiv : "<%= @controlDiv %>",
        
        extraClause : "<%= @extraClause %>",
        
        modelType : <%= @modelType %>,
        modelTemplate : "<%= @modelTemplate %>",

        modal_create_title  : '<%= t "create-venue" %>',
        modal_edit_title    : '<%= t "edit-venue" %>',
        confirm_content     : '<%= (t "delete-selected-venue").html_safe %>',
        confirm_title       : '<%= t "delete-confirmation" %>',
        
        name                : '<%= t "name" %>',
        sort_order          : '<%= t "sort-order" %>',
        
        showControls       : <%= @showControls %>
        
    });
});

</script>
