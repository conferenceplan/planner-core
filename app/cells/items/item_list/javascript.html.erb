<script type="text/javascript">

jQuery(document).ready(function() {
    jQuery("#items").cpItemTable({
        caption : "<%= @caption %>",
        selectNotifyMethod : <%= @selectNotifyMethod %>,
        clearNotifyMethod : <%= @clearNotifyMethod %>,
        root_url : "<%= @root_url %>",
        baseUrl : "<%= @baseUrl %>",
        getGridData : "<%= @getGridData %>",
        
        view : <%= @view %>,
        search : <%= @search %>,
        del : <%= @del %>,
        edit : <%= @edit %>,
        add : <%= @add %>,
        refresh : <%= @refresh %>,
        pager : "<%= @pager %>",
        
        extraClause : "<%= @extraClause %>",
        
    });
});

</script>
