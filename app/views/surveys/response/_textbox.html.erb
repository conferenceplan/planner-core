<% 
    args = {:class=> 'form-control', :rows => 5}
    if question.text_size
        args[:maxlength] = question.text_size
    end
%>
<%= text_area_tag "survey_response[#{ question.id }]", "#{@survey_response ? @survey_response[question.id.to_s] : ''}", args %>
