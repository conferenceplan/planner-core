<%#
%>
<%= select_tag "survey_response[#{question.id}]",
    options_for_select(question.survey_answers.collect{ |e| [e.answer, e.id]}, @survey_response ? @survey_response[question.id.to_s].to_i : nil),
    :class => question.survey_format.answer_style %>
