<%#
# TODO
#    user response
#     cloud tagging and styling
#
# If TAG then format as such ...
%>
<% content_tag :div, :class => question.survey_format.answer_style do %>
<div class='taggable grid_10'>
<div id='context' style='display:none'><%= question.tags_label %></div>
    <%= text_field_tag "survey_response[#{ question.id }]", "#{@survey_response ? @survey_response[question.id.to_s] : ''}", :size => question.text_size, :class => question.tags_label + ' ui-autocomplete-input' %>
<div class="cloud grid_12">
</div>
</div>
<%end%>
