<%#
# TODO
#    user response
#     cloud tagging and styling
%>
<% content_tag :div, :class => question.survey_format.answer_style do %>
    <%= text_field_tag "survey_response[#{ question.id }]", "#{@survey_response ? @survey_response[question.id.to_s] : ''}", :size => question.text_size %>
<%end%>
