<h1>New tag_context</h1>

<%= form_for(@tag_context, :html => {:'accept-charset' => 'UTF-8'}) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', tag_contexts_path %>