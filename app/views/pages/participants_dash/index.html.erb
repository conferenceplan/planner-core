
<div class="row">
    <div class="col-xs-12">
        <%= render_cell :context_menu, :display, :menu => {
            (t "participants") => '/pages/participants_dash/manage',
            (t "review surveys") => '/pages/participants_dash/survey_review'
        } %>
    </div>
</div>

<% if @cellname %>
    <%= render_cell "participants/#{@cellname}", :display %>
<% else %>
    <%= render_cell "participants/participant_dash", :display %>
<% end %>
