<div class='excluded_items_survey_map'>
  <div>
   
  <% if @excludedItemsSurveyMaps %>
    <% @excludedItemsSurveyMaps.each do |survey_map| %>
  	<div class='line'>
  		<%if survey_map.survey_answer_id != nil%>
  		SurveyQuestion: 
		<%= survey_map.survey_answer.answer %>
		<% end %>
  	</div>
  <% end %>
  <% end %>
  </div>
</div>

