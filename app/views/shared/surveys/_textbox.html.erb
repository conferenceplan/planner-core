
<%= text_area_tag "TTTTTT", '', :class => "form-control", :'data-fv-notempty' => (question.mandatory ? 'true' : 'false') %>

<!--
    also need the attribute name 
    data-fv-notempty="true" 
    -->
