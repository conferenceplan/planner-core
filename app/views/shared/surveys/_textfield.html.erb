
<%= text_field_tag "XXXXXX", '', :class => "form-control", :'data-fv-notempty' => (question.mandatory ? 'true' : 'false') %>

<!--
    also need the attribute name 
    data-fv-notempty="true" 
    -->
