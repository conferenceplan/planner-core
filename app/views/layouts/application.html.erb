<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Convention Planner Application" %>">
    <title><%= @page_title or 'Convention Planner' %></title>
    
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= stylesheet_link_tag params[:controller] if params[:controller] && PlannerRc1::Application.assets.find_asset( params[:controller] + ".css") %> <!-- TODO - we may not need-->
    <%= stylesheet_link_tag "cells/" + params[:cellname] if params[:cellname] && PlannerRc1::Application.assets.find_asset("cells/" + params[:cellname] + ".css") %> 
    
    <%= javascript_include_tag "application" %>
    <%= javascript_include_tag params[:controller] if params[:controller] && PlannerRc1::Application.assets.find_asset( params[:controller] + ".js") %> <!-- TODO - we may not need-->
    <%= javascript_include_tag "cells/" + params[:cellname] if params[:cellname] && PlannerRc1::Application.assets.find_asset("cells/" + params[:cellname] + ".js") %>
    
    <%= csrf_meta_tags %>
    
    <%=render 'layouts/common_js' %>
    
    <%= yield(:cssheader) if content_for?(:cssheader) %>
    <%= yield(:jsheader) if content_for?(:jsheader) %>
    
    <style type="text/css">
        <%= yield(:styleheader) if content_for?(:styleheader) %>
    </style>
  </head>
  <body>
      <%= render partial: 'layouts/templates' %>
    <!-- %=render 'layouts/common_page_top' % -->
          <%= render 'layouts/navigation' %>
<% if current_user %>
          <%= render 'layouts/topbar' %>
<% end %>
    <main role="main">
      <div class="container-fluid">
           <div class="row-fluid">
                <div class="span12">
                    <div id="alert-area">
                        <%= render partial: 'layouts/messages', flash: flash %>
                    </div>
                    <%= content_for?(:content) ? yield(:content) : yield %>
                </div>
           </div>
      </div>
      <footer class="footer affix-bottom">
      <div class="container-fluid">
              <div class="container-fluid">
                  Cell is: 
                  <%= params[:cellname] %>
                  <br/>Locale is:
                  <%= I18n.locale %>
                  <br/><%= t :hello %>
                  <br/><%= I18n.available_locales %>
              </div>
      </div>
      </footer>
    </main>
  </body>
</html>