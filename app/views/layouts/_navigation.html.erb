<div class="navbar navbar-inverse" id="main-nav-bar">
    <nav class="navbar-inner">
        <a class="brand" href='<%= "/" + I18n.locale.to_s %>'><%= @page_title or 'Convention Planner' %></a>
        <ul class="nav pull-right">
            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                    Language
                    <b class="caret"></b>
                </a>
                <ul class="dropdown-menu">
                    <% UISettingsService.getLanguages.each do |lang| %>
                        <li><a href="<%= '/' + lang + request.fullpath %>"><%= t lang %></a></li>
                    <% end %>
                </ul>
            </li>
            <% if current_user %>
                <li><%= link_to "Edit Profile", edit_user_path(:current) %></li>
                <li><%= link_to "Logout", logout_path %></li>
            <% end %>
        </ul>
    </nav>
</div>
