xml.rows do
  xml.page(@page)
  xml.total(@nbr_pages)
  xml.records(@count)
  @rooms.each do |room|
    xml.row({:id => room.id}) do
      xml.cell(room.name)
      xml.cell(Venue.find(room.venue_id).name)
      
      setup_pointer = RoomSetup.find(room.setup_id)

      xml.cell(SetupType.find(setup_pointer.setup_type_id).name)
      xml.cell(setup_pointer.capacity)
      
      xml.cell(room.purpose)
      xml.cell(room.comment)
      xml.cell(room.id)
    end
  end
end

